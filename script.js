// 1. Pilih tombol berdasarkan ID-nya
const tombolSapa= document.getElemenByld('sapaButton');
// 2. Tambahkan 'even listner' untuk aksi 'click'
tombolSapa.addEventListener('cilik',function(){
     // 3. Jalankan kode ini ketika tombol di-klik
    alert('Halo! Terima kasih sudah berkunjung!');
});

// 1. selec the button and the body 
const themetoggleButton = document.getElemenByld('tombol-tema')
const bodyElement = document.body;
// 2. Add a 'click' event listener to the button
themetogglebutton.add
